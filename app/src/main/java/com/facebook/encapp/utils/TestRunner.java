package com.facebook.encapp.utils;

import com.facebook.encapp.proto.Test;

public class TestRunner {
    public TestRunner(Test test) {
        // create a source
        // Create x sinks (no,visible,encoded)
    }
}
